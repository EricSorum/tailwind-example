export type FeatureType = {
  id: number;
  img: string;
  txt: string;
}

export type NavType = {
  id: number;
  txt: string;
  url: string;
}

export type SocialType = {
  id: number;
  icon: string;
  url: string;
}

export type ProductType = {
  id: number;
  img: string;
  url: string;
  alt: string;
}